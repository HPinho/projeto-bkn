module core::counter;

pub struct Counter {
    value: i64;
}

pub fn sum(limit: i64) -> i64 {
    let total: i64 = 0;
    let index: i64 = 0;
    while index < limit {
        total = total + index;
        index = index + 1;
    }
    return total;
}
